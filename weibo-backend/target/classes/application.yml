server:
  port: 8080

spring:
  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver
    url: jdbc:mysql://localhost:3306/weibo_platform?useUnicode=true&characterEncoding=utf8&useSSL=false&serverTimezone=Asia/Shanghai&allowPublicKeyRetrieval=true
    username: root
    password: Xt214998
  
  servlet:
    multipart:
      enabled: true
      max-file-size: 10MB
      max-request-size: 50MB

  web:
    resources:
      static-locations: file:uploads/

mybatis-plus:
  configuration:
    map-underscore-to-camel-case: true

jwt:
  secret: weiboplatform2024secretkey123ABCDEF
  expiration: 86400000
